//DOM Manipulation
/** document is the DOM object we want to access and this is an example how we change HTML
 *
 * getElementById() is a method we used to access the id of an element
 * innerHTML is a property, used to get or change the content of HTML elements
 */
document.getElementById("header-title").innerHTML = "Hi!";
